# 蘇杭開發官網｜GitHub Pages 版

上傳方式：
1. 將 `index.html` 與整個 `images` 資料夾上傳到 GitHub repository 根目錄。
2. GitHub → Settings → Pages → Deploy from a branch。
3. Branch 選 `main`，資料夾選 `/ (root)` 後儲存。

注意：請保持 `images` 資料夾名稱與檔案位置不變，否則圖片會無法顯示。
